
Map Reduce avec SQL
-------------------

Le langage :epkg:`SQL` ne permet pas de faire
autant de choses que :epkg:`Spark` mais il est
dans sa logique assez proche de :epkg:`map/reduce`.
Ce constat a abouti au langage :epkg:`Hive` ou
:epkg:`presto`.

